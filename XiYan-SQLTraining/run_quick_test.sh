#!/bin/bash
# Example usage of the quick_test.py script

# Replace these paths with your actual model and adapter paths
MODEL_PATH="/path/to/your/model"
ADAPTER_PATH="/path/to/your/adapter" # Optional

# Run with merged model or base model
# python quick_test.py --model_path "$MODEL_PATH"

# Run with base model + adapter
# python quick_test.py --model_path "$MODEL_PATH" --adapter_path "$ADAPTER_PATH"
